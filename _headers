# ============================================================================
# MentisViva — Cloudflare Pages headers
# Docs: https://developers.cloudflare.com/pages/configuration/headers/
# ============================================================================

# --- Security headers (apply to ALL responses) ---
/*
  X-Frame-Options: SAMEORIGIN
  X-Content-Type-Options: nosniff
  Referrer-Policy: strict-origin-when-cross-origin
  Permissions-Policy: geolocation=(), microphone=(), camera=(), payment=()
  X-XSS-Protection: 1; mode=block

# --- Static assets (logos, uploaded portadas, fonts, etc.) ---
# Inmutables y de larga duración — nunca cambian, los hash-bust en su URL.
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# --- CSS files ---
# Cache corto + revalidación obligatoria. Antes era 30 días pero eso causaba
# que cambios de diseño no llegaran al usuario hasta semanas después
# (Safari iOS y Chrome cachean agresivo). Ahora cada hora el navegador
# valida con ETag → si no cambió, 304 (no descarga). Si cambió, descarga.
/css/*
  Cache-Control: public, max-age=3600, must-revalidate

# --- JavaScript ---
# 1 hora — mismo razonamiento. ETag-based revalidation evita downloads
# innecesarios pero garantiza propagación rápida de fixes.
/js/*
  Cache-Control: public, max-age=3600, must-revalidate

# --- HTML pages ---
# Cache corto: el contenido se renderiza vía CMS (D1+KV), así que
# revalidar cada 5 minutos balancea velocidad y frescura.
/*.html
  Cache-Control: public, max-age=300, must-revalidate

# --- Manifest, robots, sitemap ---
/manifest.json
  Cache-Control: public, max-age=86400
  Content-Type: application/manifest+json
/robots.txt
  Cache-Control: public, max-age=86400
/sitemap.xml
  Cache-Control: public, max-age=86400
  Content-Type: application/xml; charset=utf-8

# --- Admin / private pages: no cache, no index ---
/unidos.html
  Cache-Control: no-store, no-cache, must-revalidate
  X-Robots-Tag: noindex, nofollow
/cuenta.html
  Cache-Control: no-store, no-cache, must-revalidate
  X-Robots-Tag: noindex
/tracking.html
  Cache-Control: no-store, no-cache, must-revalidate
  X-Robots-Tag: noindex
/encuesta.html
  Cache-Control: no-store, no-cache, must-revalidate
  X-Robots-Tag: noindex
